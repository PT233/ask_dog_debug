# Issue tracker

This repository uses local Markdown tickets under `.scratch/<feature>/issues/`.

Each ticket is one file, numbered in dependency order. `Blocked by` declares its edges. Work any ticket whose blockers are complete. This avoids creating transient issues against the repository while its history is being replaced.

