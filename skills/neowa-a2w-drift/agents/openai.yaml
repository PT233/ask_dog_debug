interface:
  display_name: "Neowa A2W drift scout"
  short_description: "Internal A2W drift evidence scout"
  default_prompt: "Use $neowa-a2w-drift only for the hypothesis assigned by $ask-dog-debug."
policy:
  allow_implicit_invocation: false
