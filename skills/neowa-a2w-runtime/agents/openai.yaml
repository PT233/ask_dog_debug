interface:
  display_name: "Neowa A2W runtime scout"
  short_description: "Internal A2W runtime evidence scout"
  default_prompt: "Use $neowa-a2w-runtime only for the hypothesis assigned by $ask-dog-debug."
policy:
  allow_implicit_invocation: false
