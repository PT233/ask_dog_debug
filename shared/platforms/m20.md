# M20 platform knowledge

Status: `not-ready`.

The role contracts and EvidencePacket schema are available, but M20 board roles, paths, commands, thresholds, ROS topology, deployment fingerprints, and hardware interfaces have not been verified on a real robot.

For a registered M20 identity, collect only generic read-only identity and environment observations. Return `inconclusive` with the missing platform evidence. Do not import A2W board names, paths, nodes, topics, thresholds, topology, or prior cases, and do not enter RootCauseProven.

Populate this pack and `topologies/m20/` from real-machine evidence before changing status to `ready`.

